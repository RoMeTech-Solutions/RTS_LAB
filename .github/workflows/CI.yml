name: TestLab

on:
  workflow_dispatch:

jobs:
  HelloWorld:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Print Hello Word
        run: echo "$(cat hello_world.txt)"
